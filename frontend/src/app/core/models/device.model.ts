export interface DeviceInfo {
  deviceId: string;
  appId: string;
  timestamp: number;
  userAgent: string;
  platform: string;
  language: string;
  screenResolution: string;
  timezone: string;
  hardwareConcurrency: number;
}